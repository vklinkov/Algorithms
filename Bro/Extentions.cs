using System;

namespace Bro
{
    public class Extentions
    {
        public bool Swap(string str)
        {
            if (str.Length > 0)
            {
                return true;
            }

            return false;
        }
    }
}
